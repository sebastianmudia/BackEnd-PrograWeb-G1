# En producción (Vercel + Neon) solo necesitas DATABASE_URL.
# Vercel la genera automáticamente al conectar Neon.
# En local, si dejas DATABASE_URL vacío, se conecta al Postgres local
# (base 'ulima_eventos', usuario 'postgres', password '1234' — cámbialo en config/database.js).

DATABASE_URL=

JWT_SECRET=ulima-eventos-secreto-muy-largo-2026

PORT=3005
