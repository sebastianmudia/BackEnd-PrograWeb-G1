# ============================================================
# .env.example
# En producción (Vercel) solo necesitas DATABASE_URL
# Vercel la genera automáticamente al conectar Neon
# ============================================================

# Opción 1: Producción — Neon te da esta cadena completa
DATABASE_URL=postgresql://usuario:password@host.neon.tech/dbname?sslmode=require

# Opción 2: Local — variables separadas (si DATABASE_URL está vacío)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ulima_eventos
DB_USERNAME=postgres
DB_PASSWORD=tu_password_local

# JWT
JWT_SECRET=ulima-eventos-secreto-muy-largo-2026

# Puerto local
PORT=3005
